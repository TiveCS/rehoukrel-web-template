Start development servers for both frontend and backend in parallel.

Run these commands:
1. In a new terminal, cd to apps/backend and run: bun run dev
2. In another terminal, cd to apps/web and run: bun run dev

Expected results:
- Backend running on http://localhost:3000 (or configured port)
- Frontend running on http://localhost:3000 (Vite dev server)
