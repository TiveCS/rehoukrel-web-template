Run linting and formatting checks for both projects.

Steps:
1. Backend: cd apps/backend && check if there's a lint script, run it
2. Frontend: cd apps/web && bun run check (runs biome check)

Report any violations with file paths.
