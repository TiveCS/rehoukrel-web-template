Run database migrations for the backend.

Steps:
1. cd apps/backend
2. Run: bun run db:generate (generate migrations from schema)
3. Run: bun run db:push (apply migrations to database)

Confirm migrations are applied successfully.
